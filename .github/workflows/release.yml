name: Release

on:
  push:
    tags: ["v[0-9]+.[0-9]+.[0-9]+*"]

permissions:
  contents: write

jobs:
  ci:
    name: Pre-release checks
    uses: ./.github/workflows/ci.yml
    permissions:
      contents: read

  release:
    name: Build & Release
    needs: ci
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            pkg/serve/frontend/package-lock.json
            pkg/serve/frontend-next/package-lock.json

      - name: Validate stable SemVer tag
        run: |
          [[ "${GITHUB_REF_NAME}" =~ ^v(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)$ ]] || {
            echo "Release tags must be stable SemVer (vMAJOR.MINOR.PATCH), got ${GITHUB_REF_NAME}" >&2
            exit 1
          }

      - name: Build frontend
        working-directory: pkg/serve/frontend
        run: |
          npm ci
          npm run build

      - name: Build frontend-next
        working-directory: pkg/serve/frontend-next
        run: |
          npm ci
          npm run build

      - uses: goreleaser/goreleaser-action@v7
        with:
          version: "~> v2"
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
